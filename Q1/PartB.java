   public int dogWalkShift(int startHour, endHour){
        int amount = 0;
        for(int i = startHour; i <= endHour; i++ ){
            if(maxDogs == walkDogs(i){
                amount += 5 * maxDogs + 3;
            if(dogsWalked == maxDogs || (i >=9 && i <= 17)){
                amount +=3;
            }
        
            
        }
        return amount;
    }
